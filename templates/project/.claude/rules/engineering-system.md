# Engineering System

`.engsys/project.yaml`은 이 프로젝트의 표준 계약이다.

- 구조적 변경 전에는 `engsys:classify-change`를 사용한다.
- 생성 문서와 frozen historical record는 선언된 lifecycle을 따른다.
- project manifest의 native verify command를 정본으로 사용한다.
